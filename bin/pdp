#!/usr/bin/env bash

DICT=~/.pdp
HIST=~/.pdp_history

touch $DICT
touch $HIST

if [ "$1" == "-" ]; then
    c=$(cat $DICT $HIST - | pdp.py)
else
    c=$(echo $@ | cat $DICT $HIST - | pdp.py)
fi

echo $c >> $HIST
echo $c
echo "$(cat $HIST | tail -5)" > $HIST
$c
